# Release report — o-coding-navigation@0.7.0-beta.4

> Date: 2026-06-30 · Tag: `v0.7.0-beta.4` · Channel: npm `latest` + `beta`（`alpha` 保留在 `0.1.0-alpha.2`）
> SOP profile: `default-ai-coding-sop@0.7.0`（不变）。
> 版本号：`0.7.0-beta.3 → 0.7.0-beta.4`。

## What's in this release

**就绪门 DEFERRED 可见性补齐（AM-014 收尾，code-quality 审查发现）。** 纯文本可见性修复，
不改就绪门行为（beta.3 的核心准时激活逻辑不变）。

- `ocn readiness list` 概要计数加入 `DEFERRED`（此前只 brief 显示、list 漏了）：
  `PASS … / WAIVED … / DEFERRED N / FAIL … / UNKNOWN … / NA …`。
- `ocn readiness list` 的 `[DEFERRED]` 行补上到期点：`→ not due until state_X`。

两处令"延迟（未到期）检查"在所有面都不被低估——守住"不缺失"的知情承诺。

## Verification

- `prepublishOnly` 门通过：lint + typecheck + `test:coverage` + build。
- **1302** 测试全绿（无新增/删除测试；改动为渲染/概要串）。
- 实测：`ocn readiness list` 概要显示 `DEFERRED 8`，行显示 `→ not due until state_spec`。

## References

- Amendment：`docs/amendments/2026-06-30-readiness-timing-amendment.md`（AM-014）。
- 前序版本：`0.7.0-beta.3`（就绪门准时激活 AM-014/DEC-040 核心）。
