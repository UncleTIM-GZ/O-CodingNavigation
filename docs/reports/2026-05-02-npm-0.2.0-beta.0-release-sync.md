# npm + GitHub Release Sync Report — `o-coding-navigation@0.2.0-beta.0`

> Generated: 2026-05-02
> Authoring branch: `release/sop-0.2.0-beta.0-sync`
> Status: beta release; not GA; not production-ready

---

## 1. Summary

- Version bumped from `0.1.0-beta.0` to `0.2.0-beta.0` because the SOP runtime profile is now `default-ai-coding-sop@0.2.0` and the npm registry forbids re-publishing the same version.
- `npm publish --tag beta` succeeded against `https://registry.npmjs.org/`.
- Both `latest` and `beta` npm dist-tags now resolve to `0.2.0-beta.0`. `alpha` is preserved at `0.1.0-alpha.2` for historical use.
- Annotated git tag `v0.2.0-beta.0` created and pushed; matching GitHub pre-release published.
- Active install docs (`README.md`, `docs/quickstart.md`, `docs/mcp-usage.md`) updated to recommend `npm install -g o-coding-navigation` (now safe because `latest` points at the SOP 0.2.0 mainline). The explicit prerelease pin `npm install -g o-coding-navigation@beta` is still documented.
- This release is **beta only**. No GA claim, no production-ready claim, no Cursor-verified claim, no Cline-verified claim. Validated host remains Claude Desktop on Windows with WSL2.

---

## 2. Source basis

- PR #58 — `examples/plan-to-verify` smoke covering all 19 SOP 0.2.0 steps, merged to `main` as `a07a361`.
- PR #57 — `feat(sop): cut over runtime to 0.2 full flow` (runtime cutover).
- Plan-to-verify report: [`docs/reports/2026-05-02-sop-0.2-plan-to-verify-example.md`](./2026-05-02-sop-0.2-plan-to-verify-example.md).

---

## 3. Version bump

Done with a Node one-liner (no `npm version`, no auto-tag). Lockfile resync via `npm install --package-lock-only`.

`git diff -- package.json package-lock.json` summary:

- `package.json`: only `version` changed (`0.1.0-beta.0` → `0.2.0-beta.0`).
- `package-lock.json`: only top-level `version` and `packages[""].version` changed (mirror of the same bump). No dependency add / remove / update.

---

## 4. Pre-publish checks

| Check | Result |
|---|---|
| `npm ci` | clean install, 304 packages |
| `npm run lint` | OK |
| `npm run typecheck` | OK |
| `npm run test` | 80 test files, 650 tests passed |
| `npm run test:coverage` | 85.91% lines (passes the publish-time gate) |
| `npm run build` | OK (`tsc -p tsconfig.build.json` + chmod) |
| `bash examples/plan-to-verify/scripts/smoke.sh` | walked all 19 steps; ended at `state_verify / step_final_build_verdict` |
| `npx vitest run tests/e2e/example-plan-to-verify.test.ts` | 3 tests passed |
| Local install smoke (working copy) | `ocn --version` printed `0.2.0-beta.0`; fresh init wrote `sopProfileVersion: 0.2.0`; `.ocoding/artifacts.yaml` references `docs/18-final-build-verdict.md`; `ocn-mcp` stayed silent on success path |
| `npm pack --dry-run` | `o-coding-navigation@0.2.0-beta.0` tarball; 299 files; included `dist/`, `LICENSE`, `README.md`, `package.json`, `docs/quickstart.md`, `docs/mcp-usage.md`; **no `src/`, `tests/`, `examples/`, `docs/reports/`, `docs/plans/`, `.github/`, `.ocoding/`, `node_modules/`, `coverage/`, `.env`, or private keys** |

`prepublishOnly` re-ran lint + typecheck + coverage + build at publish time and all four passed before npm uploaded the tarball.

---

## 5. npm publish evidence

- Command: `npm publish --tag beta`
- Registry: `https://registry.npmjs.org/`
- Publisher: `uncletimgz` (`npm whoami`)
- Output (final lines):
  ```
  npm notice name: o-coding-navigation
  npm notice version: 0.2.0-beta.0
  npm notice filename: o-coding-navigation-0.2.0-beta.0.tgz
  npm notice package size: 142.9 kB
  npm notice unpacked size: 513.9 kB
  npm notice total files: 299
  npm notice Publishing to https://registry.npmjs.org/ with tag beta and default access
  + o-coding-navigation@0.2.0-beta.0
  ```
- Package URL: https://www.npmjs.com/package/o-coding-navigation/v/0.2.0-beta.0

---

## 6. npm latest movement

- Command: `npm dist-tag add o-coding-navigation@0.2.0-beta.0 latest`
- Output: `+latest: o-coding-navigation@0.2.0-beta.0`
- Post-state (registry, with cache cleared):
  ```json
  {
    "latest": "0.2.0-beta.0",
    "alpha": "0.1.0-alpha.2",
    "beta": "0.2.0-beta.0"
  }
  ```
- `npm view o-coding-navigation@latest version` → `"0.2.0-beta.0"`
- `npm view o-coding-navigation@beta version` → `"0.2.0-beta.0"`
- `npm view o-coding-navigation@alpha version` → `"0.1.0-alpha.2"` (unchanged)

---

## 7. Post-publish install smoke (against registry)

In a fresh, isolated `--prefix` directory:

- `npm install -g o-coding-navigation --prefix <SMOKE_ROOT>/npm-global` succeeded (no selector — the new `latest` resolves to `0.2.0-beta.0`).
- `ocn --version` → `0.2.0-beta.0`.
- `ocn --help` listed every shipped command.
- `ocn-mcp` started under stdin EOF and produced **zero stdout/stderr writes** (success-path silence preserved).
- `ocn init` in a fresh demo directory wrote `state.json` with `sopProfileVersion: 0.2.0`, `currentStateId: state_discovery`, `currentStepId: step_project_brief`.
- `.ocoding/artifacts.yaml` references `docs/18-final-build-verdict.md` (SOP 0.2.0 final build verdict step).
- `ocn status` reported `SOP Profile: default-ai-coding-sop@0.2.0`.

---

## 8. GitHub release evidence

- Tag: `v0.2.0-beta.0` (annotated; `git cat-file -t` returns `tag`).
- Type: GitHub pre-release (`isPrerelease: true`, `isDraft: false`).
- URL: https://github.com/UncleTIM-GZ/O-CodingNavigation/releases/tag/v0.2.0-beta.0
- Target commit: the head of `release/sop-0.2.0-beta.0-sync` containing the version bump + docs updates + this report.
- Notes file: `/tmp/ocn-v0.2.0-beta.0-release-notes.md` (mirrored as the release body).

---

## 9. Active docs update

| File | Change |
|---|---|
| `README.md` | §3 status table now reflects SOP 0.2.0 mainline; §4.1 install command is `npm install -g o-coding-navigation` with explicit `@beta` pin documented; §4.2 channel table shows `latest = 0.2.0-beta.0`, `beta = 0.2.0-beta.0`, `alpha = 0.1.0-alpha.2`; pre-GA caveat preserves the verbatim host-support line; stale `Do NOT use untagged install` warnings removed; mirrored Chinese sections (§C, §D) updated symmetrically; npm publish discipline bullet (English + Chinese) refers to 0.2.0-beta.0 evidence. Dropped the obsolete "`latest` movement DEC" roadmap bullet now that the move has happened. |
| `docs/quickstart.md` | §1.1 install command, expected `--version` output, dist-tag note, common-error rows; §A.1 mirrors in Chinese. |
| `docs/mcp-usage.md` | §1 / §A startup section now leads with `npm install -g o-coding-navigation`, documents the explicit `@beta` pin, and carries the verbatim host-support line. |

The exact host-support line — *"Validated with Claude Desktop on Windows with WSL2. Cursor and Cline are not yet verified."* — is preserved in all three docs.

---

## 10. Non-goals (explicit)

- **No GA claim.** This is `0.2.0-beta.0`, not `1.0.0`. The package is still pre-GA.
- **No production-ready claim.** No "production-ready" / "stable" / "GA" wording was added anywhere.
- **No Cursor-verified claim.** No Cline-verified claim. Only Claude Desktop on Windows + WSL2 stays on the validated-host line.
- **No source code change beyond docs / package.** `src/`, `tests/`, `.github/workflows/` were not touched. The release is a pure version bump + docs sync + publish + tag.
- **No deletion of historical artifacts.** `v0.1.0-beta.0` GitHub Release and the `0.1.0-alpha.0` / `0.1.0-alpha.2` / `0.1.0-beta.0` npm versions remain published. `alpha` dist-tag remains pinned to `0.1.0-alpha.2`.

---

## 11. Next recommended step

Merge this release evidence PR after CI is green, then start real dogfood using `npm install -g o-coding-navigation`. Do not start GA until dogfood evidence exists.
