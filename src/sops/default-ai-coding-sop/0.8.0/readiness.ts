// SOP 0.8.0 readiness rulebook — inherited from 0.7.0 verbatim (AM-014's
// precise_activation flag over the frozen 0.4.0 rulebook). The Acceptance
// Backbone (AM-015) changes the acceptance-criteria section gate, not
// readiness, so the rulebook is re-exported unchanged.
export { readinessYaml } from "../0.7.0/readiness.js";
