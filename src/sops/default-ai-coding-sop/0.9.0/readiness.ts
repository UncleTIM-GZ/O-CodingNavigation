// SOP 0.9.0 readiness rulebook — inherited from 0.7.0 verbatim (same rulebook
// 0.8.0 re-exports; AM-014's precise_activation flag over the frozen 0.4.0
// rulebook). The Outcome Backbone (AM-016) adds the outcome ledger + SHIP/
// REFLECT gates, not readiness rules, so the rulebook is re-exported unchanged.
export { readinessYaml } from "../0.7.0/readiness.js";
